# Contracts — Reactivity Upgrade & Period Filter

No backend API changes are required for this feature. All interactions remain in-memory within the Svelte `transactionsStore`, so there are no REST or GraphQL contracts to document for this iteration.
